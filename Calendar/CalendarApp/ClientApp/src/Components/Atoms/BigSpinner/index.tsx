import * as React from 'react';
import { StyledSpinner } from './styles';

const BigSpinner: React.FC = () => {
  return <StyledSpinner color="primary" type="grow" children="" />;
};

export default BigSpinner;
